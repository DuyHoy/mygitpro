import React from 'react';
import PropTypes from 'prop-types';

MenuBar.propTypes = {

};

function MenuBar(props) {
    return (
        <div>
            MenuBar
        </div>
    );
}

export default MenuBar;